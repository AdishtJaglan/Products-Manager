<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center text-primary"><%= category %> Products!</h1>
        <ul class="list-group">
            <% for(let p of product) {%>
                <li class="list-group-item">
                    <a href="/products/<%= p.id %>" class="text-decoration-none text-dark"><%= p.name %></a>
                </li>
            <% } %>
        </ul>

        <div class="mt-4">
            <b><a href="/products/new" class="btn btn-success">Create a New Product</a></b>
            <br>
            <b><a href="/products" class="btn btn-secondary">See All Products</a></b>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"></script>
</body>

</html>